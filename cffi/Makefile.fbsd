#### -*- Mode: Makefile -*-

### Not tested!

OBJ_SUFFIX=o
SHLIB_SUFFIX=so
LIB_NAME=decNumber
SHLIB_NAME=lib$(LIB_NAME)

CC=gcc
SHFLAGS=-shared -fPIC -O3
CFLAGS+=$(SHFLAGS) -Wall
LDFLAGS+=$(SHFLAGS) -lc -Wl,-soname,$(SHLIB_NAME).$(SHLIB_SUFFIX)

DECNUMBER_CFLAGS=$(CFLAGS) -D DECLITEND=$(DECLITEND) -D DECDPUN=$(DECDPUN)
DECNUMBER_LDFLAGS=$(LDFLAGS) -D DECLITEND=$(DECLITEND) -D DECDPUN=$(DECDPUN)
